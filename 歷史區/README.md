# MemoPen 歷史區

這裡收存 MemoPen 的舊版號檔案。GitHub 根目錄只保留最新版，舊版依版本號分資料夾保存。

## 已收存版本

- `V0.14/`：原始 `MemoPen_V0.14_Nuitka_Package.zip` 內容。
- `V0.14.1/`：上一版 Git 發布檔案，包含 `.py`、`.pyw`、Nuitka 建置檔、說明與文件。

## 最新版

最新版請回到 repo 根目錄使用 `MemoPen_V0.14.2.py` 或 `MemoPen_V0.14.2.pyw`。
